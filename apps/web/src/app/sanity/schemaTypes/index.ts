import { page } from './page-schema';
import { person } from './person';
import { post } from './post';
import { settings } from './settings';

export const schemaTypes = [post, person, settings, page];
