{
  "name": "@shared",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "bun run tsc -p tsconfig.json",
    "dev": "bun run tsc -w -p tsconfig.json",
    "lint": "eslint src --ext .ts",
    "test": "bun test",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "@types/bun": "^1.1.12",
    "typescript": "^5.6.3"
  }
}

